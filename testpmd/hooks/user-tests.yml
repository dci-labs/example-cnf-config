---
- name: Run migration test
  include_role:
    name: example-cnf-validate
  when: run_migration_test|default(true)|bool

- name: Get logs from pods
  include_role:
    name: get-logs-from-namespace
  vars:
    get_logs_ns: example-cnf
    get_logs_folder: "{{ job_logs.path }}"
